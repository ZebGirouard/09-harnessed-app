Read `README.md` and `TASK.md` before coding.

If you get stuck, trace one request at a time:
1. `loadIssues()` should call the GET route and then pass the result into `renderIssues(...)`.
2. The submit handler should POST the form data to the same API family.
3. The server should validate the title before creating a new issue.

If you can describe those three steps clearly, you are close.
