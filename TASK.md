# Task Card

## Goal
Finish the issue tracker flow with the smallest useful full-stack change.

## Inputs
- `GET /api/issues`
- `POST /api/issues`
- the starter form in `public/index.html`
- the checks in `tests/issues.test.mjs`

## Output
- a working page that loads and creates issues without a page refresh

## Success Check
1. Start the server with `npm run dev`
2. Open the page and confirm seed issues load
3. Submit a new issue and confirm it appears immediately
4. Run `npm test`
